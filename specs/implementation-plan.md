---
id: implementation-plan
phase: 3
agent: Architect
status: Approved
created: "2026-04-24"
updated: "2026-04-24"
version: "1.0.0"
approved_by: "Samuel Combey"
approval_date: "2026-04-24"
upstream_refs:
  - specs/prd.md
  - specs/architecture.md
  - specs/challenger-brief.md
  - specs/product-brief.md
  - specs/codebase-context.md
  - specs/typescript-rewrite-plan.md
dependencies:
  - prd
  - architecture
risk_level: high
owners:
  - Samuel Combey
sha256: null
---

# Implementation Plan

> **Phase:** 3 -- Solutioning (output) / Phase 4 -- Implementing (working document)
> **Agent:** Generated by The Architect (Turn 2), executed by The Developer
> **Status:** Approved (Turn 2 of 2-turn Architect phase)
> **Created:** 2026-04-24
> **Approval date:** 2026-04-24
> **Approved by:** Samuel Combey
> **Upstream:** [`specs/prd.md`](prd.md) · [`specs/architecture.md`](architecture.md) (sealed v1.0.2)

---

## Plan Summary

| Attribute | Value |
|-----------|-------|
| **Total Milestones** | 12 (M0 through M11) |
| **Total Stages** | 6 (Setup, Foundational, Detection, Module Port, Cutover, Polish) |
| **Estimated Duration** | 9–12 months (per Product Brief timeline revision — NOT the rewrite plan's 6.5-mo stretch target) |
| **Primary Execution Model** | Strangler-fig: 1.2 → 1.3 → … → 1.N patch/minor ships with zero behavior change, then 2.0.0-rc.x → 2.0.0 cutover |
| **Total Modules to Port** | 159 lib modules + 1 CLI dispatcher + 4 runner/auxiliary entries = 164 source files |
| **CLI Commands Preserved** | 147 leaf branches (argv depth 2–5) |
| **Test Corpus Preserved** | **84 test files on disk; 83 active in `npm test`** (`test-agent-intelligence.test.js` excluded in `vitest.config.js`); 1,930 assertions (strangler ratchet) |
| **Parallel Execution Branches** | Tasks marked `[P]` may run concurrently within a stage |
| **Critical Path** | M0 → M1 → M2 → M3 + M4 → M5 → M6 → M7 → M8 → M9 → M10 (M11 runs parallel with M0-M10) |
| **ADRs Referenced** | 12 (`specs/decisions/adr-001-*.md` through `adr-012-*.md`) |

---

## Execution Key

- **`[P]`** — Task can run in parallel with other `[P]`-marked tasks in the same stage (different files, no shared mutable state, no dependencies between them)
- **`[E*-S*]`** — Traces to PRD epic story (e.g. `E1-S4` = Epic 1 Story 4, coverage ratchet)
- **`[ADR-NNN]`** — Task implements a decision from the referenced ADR
- **`[P0]` through `[P9]`** — Maps to `specs/typescript-rewrite-plan.md` §7 phase numbering
- **`Blocker`** — Task that must complete before the next milestone begins
- **`Checkpoint`** — Human approval gate before subsequent stage begins

---

## Milestone 1: M0 — Tooling Foundation (Stage 1 — Setup + Stage 2 — Foundational)

**Goal:** CI gates + lint + coverage ratchet + CLI help snapshots + contract-slash-command test are green on v1.1.14 baseline. Zero behavior change to the JavaScript runtime. Every subsequent milestone depends on M0.

**Rewrite Plan phase:** P0 (Tooling foundation).

**Stories covered:** E1-S1 through E1-S10; E4-S1; E4-S4.

### Stage 1 — Setup

| Task ID | Deps | Story | Description | Files |
|---------|------|-------|-------------|-------|
| T1.1 | — | `[E1-S1][ADR-005]` `[P]` | Author `tsconfig.json` with strict + allowJs + NodeNext + `paths: { "@lib/*": ["bin/lib-ts/*", "bin/lib/*"] }` strangler alias. **Acceptance gate**: commit `tests/test-paths-alias-smoke.test.ts` — a ≤10-line fixture importing `@lib/io` (resolves to `bin/lib/io.js` pre-port, `bin/lib-ts/io.ts` post-port); `tsc --noEmit` exits 0 in both states. Wired into `typescript.yml`, runs from M0 onwards (NOT dormant) | `tsconfig.json`, `tests/test-paths-alias-smoke.test.ts` |
| T1.2 | — | `[E1-S3]` `[P]` | Author `biome.json` with `lint/suspicious/noExplicitAny` as error, format rules, TS+JS scope | `biome.json` |
| T1.3 | — | `[E6-S5]` `[P]` | Scaffold `CHANGELOG.md` with retroactive 1.1.14 entry covering chunker + tracer fixes | `CHANGELOG.md` |
| T1.4 | — | `[E1-S9]` `[P]` | Author PR template with zero-behavior-change checkbox | `.github/pull_request_template.md` |
| T1.5 | — | `[E1-S10]` `[P]` | Author Dependabot config: weekly grouped version-updates; immediate security-updates with CI auto-merge on audit-level=high (per ADR-008 / SEC-003) | `.github/dependabot.yml` |
| T1.6 | T1.1 | `[E1-S1]` | Install TypeScript + `@types/node@^24` + `@biomejs/biome@^2.4.13` as devDeps; update `package-lock.json` | `package.json`, `package-lock.json` |

**Checkpoint C1:** Run `npx tsc --noEmit` + `npx biome check` locally against v1.1.14 JS; both pass with 0 errors. No behavior change to runtime.

### Stage 2 — Foundational Infrastructure

| Task ID | Deps | Story | Description | Files |
|---------|------|-------|-------------|-------|
| T2.1 | T1.6 | `[E1-S2][ADR-001]` | Author `tsdown.config.ts` (pinned `tsdown@0.21.10` exact) with entry points, shebang banner, .d.ts emission, source maps. **Acceptance gate (live, not dormant)**: commit `bin/lib-ts/_smoke.ts` — a stub ≤10-line typed module exporting one function; `npm run build` produces `dist/lib/_smoke.js` (with shebang preserved on entry points) + `dist/lib/_smoke.d.ts` + source map; `tests/test-build-smoke.test.ts` asserts all three artifacts exist and parse. Wired into `typescript.yml`. ADR-001 specifies fallback config if tsdown is displaced mid-rewrite | `tsdown.config.ts`, `bin/lib-ts/_smoke.ts`, `tests/test-build-smoke.test.ts`, `package.json` |
| T2.2 | T1.6 | `[E1-S4]` | Capture coverage baseline via `npm test --coverage --reporter=json-summary`; commit `tests/coverage-baseline.json`; author `scripts/check-coverage-ratchet.mjs` | `tests/coverage-baseline.json`, `scripts/check-coverage-ratchet.mjs` |
| T2.3 | — | `[E1-S5]` | Expand `.github/workflows/quality.yml` path filter to include `bin/**`, `src/**`, `package.json`, `install.sh`; add Node 22 + Node 24 matrix | `.github/workflows/quality.yml` |
| T2.4 | T1.6, T2.2 | `[E1-S6]` `[P]` | Author `.github/workflows/typescript.yml` — runs `tsc --noEmit`, `biome check`, coverage ratchet, on every PR | `.github/workflows/typescript.yml` |
| T2.5 | — | `[E1-S7]` `[P]` | Author `.github/workflows/e2e.yml` — runs `node bin/holodeck.js --scenario baseline` | `.github/workflows/e2e.yml` |
| T2.6 | — | `[E1-S8]` `[P]` | Wire `npm audit --audit-level=high` gate into `quality.yml`; author `.github/workflows/audit.yml` weekly cron | `.github/workflows/quality.yml`, `.github/workflows/audit.yml` |
| T2.7 | — | `[E4-S1]` `[P]` | Capture CLI help snapshots for all 147 subcommands via `scripts/capture-cli-snapshots.mjs`; commit `tests/golden-masters/cli-help/*.txt` | `scripts/capture-cli-snapshots.mjs`, `tests/golden-masters/cli-help/` |
| T2.8 | — | `[E4-S4]` `[P]` | Author `contracts/slash-commands.json` enumerating `.jumpstart/agents/*.md` ↔ CLAUDE.md slash-command table; author `tests/test-slash-command-contract.test.js` | `contracts/slash-commands.json`, `tests/test-slash-command-contract.test.js` |
| T2.9 | — | `[E2-S5][ADR-006]` | Author commit-msg hook at `.husky/commit-msg` enforcing conventional-commits vocabulary `{feat, fix-drift, fix-logic, chore, docs, test, build, ci, refactor, perf}` + Merge-allowlist; wire via `package.json` `prepare` script | `.husky/commit-msg`, `package.json`, `tests/test-commit-msg-hook.test.js` |
| T2.9b | T2.7 | `[E4-S2]` `[P]` | Author `scripts/diff-cli-help.mjs` CI step that diffs `bin/cli.js <cmd> --help` against committed snapshots on every PR | `scripts/diff-cli-help.mjs`, update to `quality.yml` |
| T2.9c | — | `[E2-S5b]` `[P]` | Author `.github/workflows/pr-title-lint.yml` validating PR titles against conventional-commits at PR-open time (squash-merge enforcement) | `.github/workflows/pr-title-lint.yml` |
| T2.9d | — | `[E2-S5]` `[P]` | Author `scripts/audit-no-verify-commits.mjs` + wire into `audit.yml` weekly cron | `scripts/audit-no-verify-commits.mjs`, update to `audit.yml` |
| T2.10 | — | `[NFR-P02]` `[P]` | Author `scripts/bench-cli-startup.mjs` (100-invocation benchmark, P95 report); capture v1.1.14 baseline | `scripts/bench-cli-startup.mjs`, `tests/baselines/cli-startup.json` |
| T2.11 | — | `[NFR-P03]` `[P]` | Author `scripts/bench-test.mjs` (3-iteration median); capture v1.1.14 baseline | `scripts/bench-test.mjs`, `tests/baselines/test-wallclock.json` |
| T2.12 | — | `[NFR-P04]` `[P]` | Wire `npm pack --dry-run` size assertion (≤1.5 MB) into `audit.yml` | `.github/workflows/audit.yml` |
| T2.13 | T2.1 | `[ADR-001][SEC-005]` | Post-build shebang-preservation CI assertion: `head -c 19 dist/cli.js \| diff - <(echo '#!/usr/bin/env node')` step in `typescript.yml` (dormant until first port ships dist/) | `.github/workflows/typescript.yml` |
| T2.14 | T2.2–T2.12 | `[E1]` | Author `scripts/verify-baseline.mjs` aggregating: full-suite green; holodeck `--scenario baseline` PASS; CLI-snapshot diff clean; `npm audit --audit-level=high` clean. Outputs structured JSON pass/fail summary. Wired into Checkpoint C2 as M0 acceptance gate | `scripts/verify-baseline.mjs` |

**Checkpoint C2 / Gate M0 → M1:** All CI workflows green on v1.1.14 unchanged. `scripts/verify-baseline.mjs` (new) aggregates: unit tests green, holodeck baseline PASS, CLI help snapshots diff-clean, no new npm audit high findings. Samuel approves M0 before M1 begins.

---

## Milestone 2: M1 — Contract-Drift Detection Infrastructure (Stage 3)

**Goal:** All detection gates wired and green on v1.1.14 baseline. Cross-module contract integration harness detects the SimulationTracer-class bug on a synthetic fixture. CI strictly stronger than M0 but still zero behavior change.

**Rewrite Plan phase:** P0 (extended) / P1 prep.

**Stories covered:** E2-S1 through E2-S7.

### Stage 3 — Detection Infrastructure

| Task ID | Deps | Story | Description | Files |
|---------|------|-------|-------------|-------|
| T3.1 | T2.4 | `[E2-S1][ADR-006]` `[Blocker]` | Design + implement cross-module contract integration harness: AST-based declaration extraction via `typescript` compiler API for TS + `@babel/parser` for JS; call-site AST walk over `bin/lib/**/*.{js,ts}`. Emits `.jumpstart/metrics/drift-catches.json` per CI run | `tests/test-public-surface.test.ts`, `scripts/extract-public-surface.mjs` |
| T3.2 | — | `[E2-S1]` `[P]` | Commit synthetic fixture at `tests/fixtures/contract-drift/simulation-tracer-vs-holodeck/` replicating pre-fix 4-methods-vs-12-calls divergence (committed file-pair, NOT a git-ref checkout) | `tests/fixtures/contract-drift/simulation-tracer-vs-holodeck/*` |
| T3.3 | T3.1 | `[E2-S1]` | Run harness against v1.1.14 `main` — confirm zero drift incidents detected. Run against T3.2 fixture — confirm exactly 8 drift incidents reported with file:line references | `.jumpstart/metrics/drift-catches.json` |
| T3.4 | T2.4 | `[E2-S2]` `[P]` | Wire `npx tsc --noEmit` step into `typescript.yml` (if not already present from T2.4) | `.github/workflows/typescript.yml` |
| T3.5 | T2.1 | `[E2-S3][ADR-006]` `[P]` | Author `scripts/check-public-any.mjs` — AST-walk `dist/**/*.d.ts` files, fail on `TSAnyKeyword` in exported positions with `src/cli/main.ts` + `src/lib/ipc.ts` allowlist | `scripts/check-public-any.mjs`, update to `typescript.yml` |
| T3.6 | T2.1 | `[E2-S4]` `[P]` | Author `scripts/check-return-shapes.mjs` — AST linter enforcing machine-readable return shapes on exported functions with >2 return fields (JSDoc `@returns {{ shape }}` OR typed TS return signature both accepted per KU-04 QUALIFIED) | `scripts/check-return-shapes.mjs`, update to `typescript.yml` |
| T3.7 | T2.9 | `[E2-S6]` `[P]` | Author `.github/workflows/metrics-cron.yml` — weekly cron generating `.jumpstart/metrics/regression-share.json` from commit history + drift-catches rollup | `.github/workflows/metrics-cron.yml`, `scripts/count-drift-catches.mjs` |
| T3.8 | T2.1 | `[E2-S7][ADR-006]` | Author `scripts/check-process-exit.mjs` — grep `src/**/*.ts` + `dist/**/*.js` for `process.exit(`, allowlist `src/cli/main.ts` + `src/lib/ipc.ts`, fail on any other site. Dormant until first port lands | `scripts/check-process-exit.mjs`, update to `typescript.yml` |
| T3.9 | T3.1 | `[ADR-009]` `[Blocker]` | Author `bin/lib-ts/path-safety.ts`: `assertInsideRoot(p, root)` helper + `safePathSchema(boundaryRoot)` Zod factory; commit fixture matrix `tests/fixtures/security/path-traversal/` (≥6 inputs: `../etc/passwd`, absolute path, sneaky `./a/../b`, symlink-outside, null-byte, Windows traversal); test asserts each fixture triggers `ValidationError` exit-2. **Two-tier dormancy**: (a) `tests/test-path-safety.test.ts` unit tests of `assertInsideRoot()` and `safePathSchema(...)` purely against the helper — runs **immediately, NOT dormant**, from M1 onwards; (b) subprocess `dist/lib/path-safety.js` replay tests dormant until first port. **`[Blocker]` for T4.1.1** (`io.ts` and other M2 leaf ports likely consume path-typed fields and must use `safePathSchema` from day one) | `bin/lib-ts/path-safety.ts`, `tests/fixtures/security/path-traversal/`, `tests/test-path-safety.test.ts` |

**Checkpoint C3 / Gate M1 → M2:** All detection gates green on v1.1.14. Samuel approves M1. Port PRs from M2 onward must pass tsc + biome + coverage-ratchet + contract-harness + no-any-in-public-API + no-process-exit-outside-allowlist + shebang + CLI-snapshot-diff + holodeck-baseline to merge.

---

## Milestone 3: M2 — Leaf + Config Clusters Ported (Stage 4.1)

**Goal:** Zero-dependency utility layer + core config layer ported to TypeScript. `bin/lib/config-yaml.cjs` eliminated. Hand-rolled YAML parser in `config-loader.js` deleted.

**Rewrite Plan phase:** P1 (leaf utilities) + P2 (schema & validation first half).

**Stories covered:** E3-S1, E3-S2.

**Per-module port recipe** (invariant across all E3 stories — augmented per Pit Crew QA Turn-2 finding for IPC v0/v1 fixture authoring at port time, not deferred to M8):

1. Pre-flight: `tests/test-<name>.test.js` passes on v1.1.14 baseline (recorded in PR description).
2. Create `bin/lib-ts/<name>.ts` with typed signatures; absorb existing logic; use typed errors from `src/errors.ts` in place of `process.exit()`.
3. Keep named-exports CJS-shape identical; `tsdown` emits `bin/lib-ts/<name>.js` + `.d.ts` to `dist/lib/` during build.
4. `tests/test-<name>.test.js` passes **unchanged** via strangler `paths` alias (resolves `@lib/<name>` → `bin/lib-ts/*` first, falls back to `bin/lib/*`).
5. Cross-module contract harness: zero new drift.
6. Coverage ratchet: at or above baseline.
7. CLI help snapshot: byte-identical pre/post port.
8. Holodeck baseline: PASS.
9. Per-file NFR-D02 (machine-readable return shapes): applicable functions have typed `@returns` OR TS return annotations.
10. Ship in next 1.x release (patch or minor per ADR-008 semver-discipline decision).
11. **IPC v0/v1 backward-compat fixture authoring** (per ADR-007, applied at port time NOT deferred to M8): for every IPC-eligible module, commit `tests/fixtures/ipc/<name>/v0/input.json` + `v0/expected-stdout.json` (envelope without `"version"` field — v1.1.14-shape) AND `tests/fixtures/ipc/<name>/v1/input.json` + `v1/expected-stdout.json` (envelope with `"version": 1`). Replay test (`tests/test-<name>-ipc-compat.test.ts`) pipes each `input.json` to `node dist/lib/<name>.js` and asserts byte-identical stdout against `expected-stdout.json`. Authoritative naming scheme: ADR-007's `<v>/input.json` + `<v>/expected-stdout.json` (PRD E4-S3's `in.json`/`out.json` references are superseded; PM-phase will be amended in a follow-up patch — captured in Deviation Log).

### Stage 4.1 — Leaf utilities + config layer

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.1.1 | M1 green | `[E3-S1][P0]` `[P]` | Port `io.js` → `bin/lib-ts/io.ts` (critical dep of every other lib module) |
| T4.1.2 | M1 green | `[E3-S1][P0]` `[P]` | Port `hashing.js` → `bin/lib-ts/hashing.ts` |
| T4.1.3 | M1 green | `[E3-S1][P0]` `[P]` | Port `timestamps.js` → `bin/lib-ts/timestamps.ts` |
| T4.1.4 | M1 green | `[E3-S1][P0]` `[P]` | Port `locks.js` → `bin/lib-ts/locks.ts` |
| T4.1.5 | M1 green | `[E3-S1][P0]` `[P]` | Port `diff.js` → `bin/lib-ts/diff.ts` |
| T4.1.6 | M1 green | `[E3-S1][P0]` `[P]` | Port `versioning.js` → `bin/lib-ts/versioning.ts` |
| T4.1.7 | M1 green | `[E3-S1][P0]` `[P]` | Port `ambiguity-heatmap.js`, `complexity.js`, `context-chunker.js`, `artifact-comparison.js` (4 more leaf modules, per batch) |
| T4.1.8 | T4.1.1–T4.1.7 | `[E3-S2][P0][ADR-003]` | Port `config-yaml.cjs` → `bin/lib-ts/config-yaml.ts` (ESM, preserves `yaml` package's `Document` AST); author `bin/lib-ts/ipc.ts` shared helper |
| T4.1.9 | T4.1.8 | `[E3-S2][ADR-003]` | Port `config-loader.js` → `bin/lib-ts/config-loader.ts`; **delete hand-rolled `parseSimpleYaml`**, use `yaml` package unified |
| T4.1.10 | T4.1.8 | `[E3-S2]` `[P]` | Port `config-merge.js` → `bin/lib-ts/config-merge.ts` |
| T4.1.11 | T4.1.8 | `[E3-S2]` `[P]` | Port `framework-manifest.js` → `bin/lib-ts/framework-manifest.ts` |
| T4.1.12 | T4.1.8, T4.1.9, T4.1.10 | `[E3-S2]` | Legacy-fixtures regression test: 10 historical `.jumpstart/config.yaml` shapes (1.0, 1.1.0, 1.1.13, 1.1.14) parse byte-identical pre/post port; commit fixtures to `tests/fixtures/config-legacy/` |

**Checkpoint C4.1 / Gate M2 → M3:** **15 modules touched at M2** (14 ported JS→TS: io, locks, timestamps, hashing, diff, versioning, ambiguity-heatmap, complexity, context-chunker, artifact-comparison, config-yaml, config-loader, config-merge, framework-manifest; PLUS 1 net-new file authored: `bin/lib-ts/ipc.ts`); tests green; `bin/lib/config-yaml.cjs` deleted; hand-rolled YAML parser removed; historical configs parse identically. Ship as **1.2.0** (first minor after 1.1.14 baseline). Samuel approves M2.

---

## Milestone 4: M3 — Spec Integrity + Graph Clusters (Stage 4.2)

**Goal:** Spec validation + graph/traceability clusters ported. `src/schemas/generated/*.ts` codegen pipeline live.

**Rewrite Plan phase:** P2 (schema & validation completion).

**Stories covered:** E3-S3, E3-S4.

### Stage 4.2 — Spec clusters

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.2.1 | M2 | `[E3-S3][ADR-004]` | Author `scripts/generate-zod-schemas.mjs` using `json-schema-to-zod`; emit `src/schemas/generated/*.ts` from `.jumpstart/schemas/*.json` canonical; commit output (for audit + CI `git diff --exit-code` gate) |
| T4.2.2 | T4.2.1 | `[E3-S3]` | Port `validator.js` → `bin/lib-ts/validator.ts` using generated Zod for primary validation; keep JSON-Schema-walker fallback for v0 compat |
| T4.2.3 | T4.2.2 | `[E3-S3]` `[P]` | Port `spec-drift.js` → `bin/lib-ts/spec-drift.ts` |
| T4.2.4 | T4.2.2 | `[E3-S3]` `[P]` | Port `analyzer.js`, `crossref.js`, `smell-detector.js` (batch) |
| T4.2.4b | T4.2.2 | `[E3-S3][ADR-012]` `[P]` | Port `secret-scanner.js` → `bin/lib-ts/secret-scanner.ts` — required by ADR-012 redaction in `timeline.ts` (T4.3.3) and `usage.ts` (T4.3.1); pattern catalog (OpenAI/Anthropic/GitHub/AWS keys, generic credential prefixes) preserved verbatim; export `scanForSecrets(input: string): SecretMatch[]` typed signature; new `redactSecrets<T>(value: T): T` recursive helper added in same file |
| T4.2.5 | M2 | `[E3-S4]` `[P]` | Port cluster D: `graph.ts`, `traceability.ts`, `bidirectional-trace.ts`, `impact-analysis.ts`, `adr-index.ts`, `repo-graph.ts` (6 modules, no inter-cluster dependencies) |

**Checkpoint C4.2 / Gate M3:** Spec + graph clusters typed; Zod codegen pipeline in CI; `git diff --exit-code src/schemas/generated/` passes. Ship as **1.3.0**. Samuel approves.

---

## Milestone 5: M4 — Feature Batch 1 — LLM / State / UX (Stage 4.3)

**Rewrite Plan phase:** P3.

**Stories covered:** E3-S5.

### Stage 4.3 — LLM + State + UX clusters (~30 modules)

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.3.1 | M2 | `[E3-S5][ADR-011][ADR-012]` `[P]` | Port cluster E: `llm-provider.ts` (with LITELLM_BASE_URL validation per ADR-011), `model-router.ts`, `cost-router.ts`, `context-chunker.ts`, `mock-responses.ts`, `usage.ts` (with secrets-redaction integration per ADR-012 — `redactSecrets()` invoked on every log-write path; ADR-012 explicitly names `usage.ts` alongside `timeline.ts`) |
| T4.3.2 | M2 | `[E3-S5]` `[P]` | Port cluster B: `state-store.ts`, `approve.ts`, `rewind.ts`, `next-phase.ts`, `ceremony.ts`, `focus.ts` |
| T4.3.3 | M2 | `[E3-S5]` `[P]` | Port cluster H: `dashboard.ts`, `timeline.ts` (with secret-scanner integration per ADR-012), `context-summarizer.ts`, `project-memory.ts`, `role-views.ts`, `promptless-mode.ts`, `workshop-mode.ts` |
| T4.3.4 | T4.3.1 | `[E3-S5]` | Golden-master parity: `npm run emulate:architect` + `emulate:full` stdout matches pre-port baseline byte-for-byte (commit `tests/golden-masters/emulate/` baselines in T2.x if not already) |
| T4.3.5 | T4.3.1, T4.3.3, T4.2.4b | `[ADR-012]` | **ADR-012 end-to-end redaction test** — commit `tests/fixtures/security/secrets-redaction/` with 3 scenarios: (a) CLI invocation `--api-key sk-fake-test-value-1234567890` ⇒ log entry must contain `[REDACTED:openai-api-key]` not the raw key; (b) `--target ./specs/foo.md` ⇒ log entry contains `./specs/foo.md` unchanged (no false positive); (c) timeline event with nested `{auth:{token:"ghp_fakevalueXXXX"}}` ⇒ persisted as `[REDACTED:github-token]`. CI gate: post-test, `grep -r "sk-[a-zA-Z0-9]{20,}\|ghp_[a-zA-Z0-9]{36,}" .jumpstart/state/ .jumpstart/usage-log.json` returns 0 matches. Wired into `quality.yml` as a separate `secrets-redaction-e2e` step | `tests/fixtures/security/secrets-redaction/`, `tests/test-secrets-redaction-e2e.test.ts`, update `quality.yml` |

**Checkpoint C4.3 / Gate M4:** 30 modules ported; emulate parity verified. Ship as **1.4.0**.

---

## Milestone 6: M5 — Feature Batch 2 — Codebase Intel / Governance / Collaboration (Stage 4.4)

**Rewrite Plan phase:** P4.

**Stories covered:** E3-S6.

**Dependency correction per Pit Crew Reviewer**: M5 depends on **M3 + M4** (not just M4), because cluster J's `quality-graph`, `type-checker` import from cluster C's `validator`, `analyzer` (ported in M3).

### Stage 4.4 — ~60 modules across 3 clusters

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.4.1 | M3 + M4 | `[E3-S6]` `[P]` | Port cluster J (codebase intelligence, ~6 modules): `ast-edit-engine.ts`, `codebase-retrieval.ts`, `refactor-planner.ts`, `safe-rename.ts`, `quality-graph.ts`, `type-checker.ts` |
| T4.4.2 | M3 + M4 | `[E3-S6]` `[P]` | Port cluster I (enterprise/governance, ~18 modules): `compliance-packs.ts`, `risk-register.ts`, `waiver-workflow.ts`, `evidence-collector.ts`, + 14 more |
| T4.4.3 | M3 + M4 | `[E3-S6]` `[P]` | Port cluster L (collaboration/stakeholder, ~25 modules): `playback-summaries.ts`, `structured-elicitation.ts`, `chat-integration.ts`, `estimation-studio.ts`, + 21 more |

**Checkpoint C4.4 / Gate M5:** ~60 additional modules typed. ~105 of 159 total ported (66%). Ship as **1.5.0** or multiple patches per ADR-008 semver-discipline.

---

## Milestone 7: M6 — Marketplace / Installer (Stage 4.5)

**Rewrite Plan phase:** P5.

**Stories covered:** E3-S7.

### Stage 4.5 — Marketplace cluster

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.5.1 | M5 | `[E3-S7][ADR-010]` | Port `install.ts` with ZIP-slip prevention per ADR-010 (path canonicalization + reject-on-escape). Retain SHA-256 verification |
| T4.5.2 | M5 | `[E3-S7]` | Port `integrate.ts`, `registry.ts`, `upgrade.ts` |
| T4.5.3 | T4.5.1 | `[E3-S7]` | Historical-shapes regression test: 10 `.jumpstart/installed.json` shapes from earlier installs parse + round-trip byte-identical |
| T4.5.4 | T4.5.1 | `[E3-S7]` | Byte-identical install-output test: `npx jumpstart-mode install skill ignition` in a throwaway sandbox produces identical file tree pre/post port |
| T4.5.5 | T4.5.1 | `[E3-S7][ADR-010]` `[Blocker]` | Security fixtures: crafted zipslip ZIP (entries like `../../../etc/passwd`), symlink-outside ZIP, absolute-path ZIP, null-byte ZIP, legitimate control ZIP — each malicious ZIP rejected with `ValidationError` + exit 2; control extracts cleanly. **`[Blocker]` for Checkpoint C4.5** — T4.5.1 is NOT marked complete until T4.5.5's fixtures pass; prevents shipping `install.ts` without zipslip enforcement |

**Checkpoint C4.5 / Gate M6:** Marketplace ported; install byte-identical; zipslip rejected. Ship as **1.6.0**.

---

## Milestone 8: M7 — Runners (holodeck + headless) (Stage 4.6)

**Rewrite Plan phase:** P6.

**Stories covered:** E3-S8.

### Stage 4.6 — Testing/e2e infrastructure

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.6.1 | M6 | `[E3-S8]` | Port `bin/headless-runner.js` → `src/testing/headless-runner.ts`; reconcile divergence with `bin/lib/headless-runner.js` (either canonicalize or document in ADR) |
| T4.6.2 | M6 | `[E3-S8]` | Port `bin/holodeck.js` → `src/testing/holodeck.ts`; resolve `bin/lib/holodeck.js` duplicate (canonical version wins; duplicate deleted or ADR-documented) |
| T4.6.3 | M6 | `[E3-S8]` `[P]` | Port `tool-bridge.ts`, `tool-schemas.ts`, `simulation-tracer.ts`, `smoke-tester.ts`, `regression.ts` |
| T4.6.4 | M6 | `[E3-S8]` `[P]` | Port `bin/verify-diagrams.js` → `src/testing/verify-diagrams.ts`, `bin/context7-setup.js` → `src/testing/context7-setup.ts` |
| T4.6.5 | T4.6.1, T4.6.2 | `[E3-S8]` | `node dist/testing/holodeck.js --scenario baseline` PASSes; report JSON shape byte-identical to pre-port baseline capture |

**Checkpoint C4.6 / Gate M7:** Runners ported; duplicates resolved; holodeck baseline green end-to-end via the TS port. Ship as **1.7.0**.

---

## Milestone 9: M8 — CLI Dispatcher (Stage 4.7)

**Rewrite Plan phase:** P7.

**Stories covered:** E3-S9, E4-S2, E4-S3, E4-S4.

### Stage 4.7 — The 5,359-line monolith

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T4.7.0 | M7 | `[E3-S9][ADR-002]` `[Blocker]` | **ADR-002 depth-cost analysis FIRST** — author `scripts/estimate-commander-boilerplate.mjs` to enumerate the 147-leaf 4–5-level subcommand tree from `bin/cli.js`'s `subcommand === '...'` branches and compute: (a) line count of explicit `.addCommand()` chain registrations needed in `src/cli/commands/*.ts` for commander v14; (b) line count of equivalent lazy `subCommands` map for citty. Output `commander_lines`, `citty_lines`, `delta` to `.jumpstart/metrics/cli-framework-cost.json`. **Decision criterion (per ADR-002)**: if `commander_lines - citty_lines > 1000` (commander adds >1000 lines of boilerplate vs citty), execute coordinated commander→citty sweep across architecture.md + Technology Stack + C4 diagram + all earlier implementation-plan references BEFORE T4.7.1. Otherwise proceed with commander v14 | `scripts/estimate-commander-boilerplate.mjs`, `.jumpstart/metrics/cli-framework-cost.json` |
| T4.7.1 | T4.7.0 | `[E3-S9]` | Author `src/cli/main.ts` — root commander (or citty) program; import `createRealDeps()` from `src/cli/deps.ts` |
| T4.7.2 | T4.7.1 | `[E3-S9]` `[P]` | Decompose 120+ subcommands into ~30 command files in `src/cli/commands/` (one file per logical group: approve, checkpoint, install, validate, timeline, governance-cluster, analysis, etc.) |
| T4.7.3 | T4.7.2 | `[E4-S2]` | Run `scripts/diff-cli-help.mjs` — must produce 0 diffs against committed snapshots |
| T4.7.4 | T4.7.2 | `[E4-S3]` `[P]` | IPC envelope regression test suite: for every dual-mode lib module, fixture pair (`tests/fixtures/ipc/<module>/{v0,v1}/`) asserts byte-identical stdout for both envelope variants |
| T4.7.5 | T4.7.4 | `[E4-S4]` | Slash-command contract test passes post-port: 1:1 match between `.jumpstart/agents/*.md` filenames and CLAUDE.md command table |

**Checkpoint C4.7 / Gate M8:** Dispatcher ported; all 147 subcommands byte-identical; IPC envelopes backward-compat tested on every lib module. Ship as **1.8.0** (last minor before 2.0).

---

## Milestone 10: M9 — 2.0.0-rc.1 Cutover (Stage 5)

**Rewrite Plan phase:** P8.

**Stories covered:** E5-S1, E5-S2.

### Stage 5 — Cutover

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T5.1 | M8 | `[E5-S1][ADR-005]` | Flip `package.json`: `type: "module"`, `engines.node: ">=24.0.0"`, `bin: {"jumpstart-mode": "./dist/cli.js", "jumpstart": "./dist/bootstrap/init.js"}`, add `exports` map |
| T5.2 | T5.1 | `[E5-S1]` | Move `bin/lib-ts/*` → `src/lib/*` (strangler collapse); update tsconfig paths to `src/lib/*`; delete `bin/lib/*.js` (originals) and `bin/cli.js` monolith |
| T5.3 | T5.2 | `[E5-S1]` | Remove `allowJs: true` from `tsconfig.json`; `tsc --noEmit` must pass clean |
| T5.4 | T5.3 | `[E5-S2]` | Build `2.0.0-rc.1` via `tsdown`; `npm pack --dry-run` confirms ≤1.5 MB; `npm publish --tag next` |
| T5.5 | T5.4 | `[E5-S2]` | Smoke-test `npx jumpstart-mode@next --version` on fresh Node 24 sandbox: reports `2.0.0-rc.1`, exit 0 |
| T5.6 | T5.4 | `[E5-S2]` | Smoke-test on Node < 24: fails with clear engines-mismatch message (not opaque crash) |

**Checkpoint C5 / Gate M9:** 2.0.0-rc.1 on `next` tag; smoke tests pass on Node 24, fail gracefully below. `dist-tags.latest` still points at last 1.x.

---

## Milestone 11: M10 — 2-Week RC Soak + `latest` Promotion (Stage 5 continued)

**Stories covered:** E5-S3, E5-S4.

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T5.7 | M9 | `[E5-S3]` | Manual smoke-test protocol: invoke representative `jumpstart-mode@next` commands from Claude Code, Cursor, VS Code Copilot, Windsurf — confirm parity on each |
| T5.8 | T5.7 | `[E5-S3]` | ≥14-day soak window with zero filed issues; re-run smoke test weekly |
| T5.9 | T5.8 | `[E5-S4]` | `npm dist-tag add jumpstart-mode@2.0.0 latest` |
| T5.10 | T5.9 | `[E5-S4]` | Final validation: `npx jumpstart-mode --version` on clean sandbox reports `2.0.0` |

**Checkpoint C5 / Gate M10:** 2.0 promoted to `latest`. Rewrite complete.

---

## Milestone 12: M11 — Housekeeping (parallel with M0–M10)

**Rewrite Plan phase:** P9 (hardening) + parallel housekeeping.

**Stories covered:** E6-S1 (KU-04 already done), E6-S2, E6-S3, E6-S4, E6-S5.

| Task ID | Deps | Story | Description |
|---------|------|-------|-------------|
| T6.1 | — | `[E6-S1]` | ✅ DONE 2026-04-24 — KU-04 spike integrated in PRD insights; no further action |
| T6.2 | any | `[E6-S2][ADR-008]` `[Blocker]` | Run `npm owner ls jumpstart-mode`; document Samuel's publish status; coordinate with Jo Otey if not owner. **`[Blocker]` for M9** — must resolve before T5.4 (`npm publish --tag next`) starts; if unresolved, fork to `@scombey/jumpstart-mode` per ADR-008 path (c) |
| T6.3 | any | `[E6-S3][ADR-008]` | Semver discipline ADR finalized: patch-per-module vs minor-per-migration-batch |
| T6.4 | any | `[E6-S4]` | Author `docs/upgrade-to-2.0.md`: Node ≥24 requirement, ESM-only, breaking changes, migration examples |
| T6.5 | any | `[E6-S5]` | Maintain `CHANGELOG.md` throughout: every 1.x release + 2.0 cutover |

### Stage 6 — Polish (post-M10)

| Task ID | Description |
|---------|-------------|
| T6.6 `[P]` | Enable `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes` in tsconfig; fix any new errors |
| T6.7 `[P]` | Remove strangler scaffolding artifacts (unused PR templates, dual-resolution aliases if any linger) |
| T6.8 `[P]` | Final retrospective: author `specs/retrospectives/rewrite-retrospective.md` capturing what worked, what didn't, lessons for future major migrations |
| T6.9 `[P]` | **Architecture Turn-2 commitment**: author Level-3 Component Diagrams for `CLI Dispatcher` and `Core Lib` containers per architecture.md §Component Interaction Diagram diagram-level note. Output: `specs/architecture-l3-cli-dispatcher.md` + `specs/architecture-l3-core-lib.md` with `C4Component` Mermaid diagrams. Verifies via `node bin/verify-diagrams.js` |

---

## Traceability Matrix

| PRD Story | Architecture §§ | ADR(s) | Implementation Milestone(s) |
|-----------|------------------|--------|------------------------------|
| E1-S1 (tsconfig) | Technology Stack, Project Structure | ADR-005 | T1.1 (M0) |
| E1-S2 (build tool) | Technology Stack | ADR-001 | T2.1 (M0) |
| E1-S3 (Biome) | Technology Stack, §Code Style | — | T1.2 (M0) |
| E1-S4 (coverage ratchet) | CI/CD / NFR-Traceability | — | T2.2 (M0) |
| E1-S5 through E1-S10 | CI/CD | — | T2.3–T2.12 (M0) |
| E2-S1 (contract harness) | API Contracts, §Security | — | T3.1–T3.3 (M1) |
| E2-S2 (tsc gate) | CI/CD | — | T3.4 (M1) |
| E2-S3 (no-any-public-API) | §Code Style, CI/CD | ADR-006 | T3.5 (M1) |
| E2-S4 (return-shape linter) | API Contracts, NFR-D02 | — (no dedicated ADR; enforced by `scripts/check-return-shapes.mjs` per architecture §NFR-D02 + KU-04 QUALIFIED verdict) | T3.6 (M1) |
| E2-S5 (commit-msg hook) | §Git Workflow | — | T2.9, T2.9c, T2.9d (M0) |
| E2-S6 (regression-share cron) | §Observability NFR-O02 | — | T3.7 (M1) |
| E2-S7 (process.exit enforcement) | NFR-R04, §Code Style | ADR-006 | T3.8 (M1) |
| E3-S1 (leaf utilities) | §Component map K | ADR-005 | T4.1.1–T4.1.7 (M2) |
| E3-S2 (config cluster) | §Component map A | ADR-003 | T4.1.8–T4.1.12 (M2) |
| E3-S3 (spec integrity) | §Component map C, §Data Model | ADR-004 | T4.2.1–T4.2.4 (M3) |
| E3-S4 (spec graph) | §Component map D | — | T4.2.5 (M3) |
| E3-S5 (LLM/state/UX) | §Component maps E, B, H | ADR-011, ADR-012 | T4.3.1–T4.3.4 (M4) |
| E3-S6 (codebase/governance/collab) | §Component maps J, I, L | — | T4.4.1–T4.4.3 (M5; **depends on M3 + M4**: codebase-intel cluster J imports from spec/graph cluster D in M3, AND from LLM/state clusters E/B/H in M4 — both predecessors required) |
| E3-S7 (marketplace) | §Component map F | ADR-010 | T4.5.1–T4.5.5 (M6) |
| E3-S8 (runners) | §Component map G | — | T4.6.1–T4.6.5 (M7) |
| E3-S9 (CLI dispatcher) | §System Components — CLI Dispatcher | ADR-002 | T4.7.0–T4.7.5 (M8) |
| E4-S1 (CLI help snapshots) | CI/CD | — | T2.7 (M0) |
| E4-S2 (snapshot diff gate) | CI/CD NFR-R02 | — | T2.9b (M0), T4.7.3 (M8) |
| E4-S3 (IPC envelope regression) | API Contracts | ADR-007 | T4.7.4 (M8) |
| E4-S4 (slash-command contract) | CI/CD | — | T2.8 (M0), T4.7.5 (M8) |
| E5-S1 (ESM flip) | §Infrastructure, Project Structure | ADR-005 | T5.1–T5.3 (M9) |
| E5-S2 (2.0.0-rc.1 publish) | §Infrastructure | ADR-008 | T5.4–T5.6 (M9) |
| E5-S3 (RC soak) | §Infrastructure | — | T5.7–T5.8 (M10) |
| E5-S4 (latest promotion) | §Infrastructure | — | T5.9–T5.10 (M10) |
| E6-S1 (KU-04 spike) | — (done) | — | ✅ done 2026-04-24 |
| E6-S2 (npm rights) | §Security | ADR-008 | T6.2 (M11) |
| E6-S3 (semver discipline) | §Infrastructure | ADR-008 | T6.3 (M11) |
| E6-S4 (upgrade doc) | Project Structure | — | T6.4 (M11) |
| E6-S5 (CHANGELOG) | Project Structure, §Documentation | — | T1.3 (M0) + T6.5 (M11) |

---

## Deviation Log

> _Developer Agent populates this section during Phase 4 execution with any task deviations from the plan above._

| Date | Task ID | Deviation | Rationale | Approved by |
|------|---------|-----------|-----------|-------------|
| 2026-04-24 | T1.4 | Vitest pinned at `^3.2.4` (latest 3.x) instead of provisional `^4.0.0` mention. | Vitest 4.0 was not released as of M0 build (latest stable: 3.2.4). 3.x already provides the `globals`, `coverage.provider: 'v8'`, and `coverage.reporter: ['json-summary']` semantics the ratchet relies on. Re-evaluate at M9 (2.0 cutover) and bump to 4.x if released and stable. No behavior change vs. plan's intent. | Samuel Combey |
| 2026-04-24 | T2.1 | tsdown `target` set to `node22` rather than the spec-default `node24`. | Strangler phase still ships under the legacy `engines: ">=14"` floor for 1.x. Building for node22 (which is also Active LTS until 2027-04) is the broadest-reach setting that still gives modern syntax. M9 retargets to `node24` simultaneously with the engines field bump. | Samuel Combey |
| 2026-04-24 | T2.2 | `@vitest/coverage-v8` added to devDependencies post-hoc (Pit Crew finding); was implicit in the original ratchet wiring. | Coverage ratchet script presumes `coverage/coverage-summary.json` exists, which requires the v8 provider plugin. The plan's T2.2 row didn't enumerate this as a separate package; we added it during sub-commit-2 remediation. | Samuel Combey |
| 2026-04-24 | T2.1 / Pit Crew | New gate `scripts/check-dist-exports.mjs` added (not in original plan). | QA Pit Crew's killer missing-gate finding: tsdown could silently drop a symbol or strip a shebang and downstream consumers wouldn't know until runtime. Wired into `verify-baseline.mjs` and runs after every build. Addition is additive — no plan tasks rescheduled. | Samuel Combey |
| 2026-04-24 | T1.2 / Pit Crew | `biome.json` `files.includes` narrowed from `**` to TS-only paths (bin/lib-ts/, src/, scripts/, tests/*.test.ts, configs). | The strict `--error-on-warnings` gate in `verify-baseline.mjs` triggered hundreds of legacy-JS lint warnings that ADR-005's strangler-fig already excludes from the rewrite scope. Narrowing the includes preserves the strict gate without dragging legacy JS into M0. Legacy `bin/lib/*.js` quality is (and always has been) gated by the existing test suite. | Samuel Combey |
| 2026-04-24 | T3.8 / Pit Crew M1 (Adversary 1) | `scripts/check-process-exit.mjs` extended to a layered 6-pattern regex check (direct, computed, alias, destructure, named-import, namespace-import). | POC by Pit Crew Adversary confirmed `process['exit'](1)`, alias `const f = process.exit; f(1)`, `import { exit } from 'node:process'`, and namespace import all bypassed the original single-pattern regex. The "right" fix is AST-based (TypeScript Compiler API). **Deferred to M2 sub-commit alongside the first real port** so the AST gate and the first non-trivial source it scans land together. The 6-pattern regex closes ~95% of the hole; the remaining 5% (string literals containing the patterns, regex literals confusing the comment stripper) is documented in the script header. | Samuel Combey |
| 2026-04-24 | T3.9 / Pit Crew M1 (Adversary 3) | `safePathSchema` Layer 1 remains lexical-only; symlink defense stays opt-in via `assertInsideRoot(p, root, { followSymlinks: true })`. | Pit Crew Adversary confirmed a symlink whose lexical path is inside the boundary but whose realpath escapes will pass the Zod refinement. The right architectural fix is a `safeReadFile`/`safeStat` family that wraps `fs.*` and applies Layer 2 unconditionally — **planned as ADR-013 for M5** alongside the IPC module port. For M1, the limitation is documented in the `safePathSchema` JSDoc and in `path-safety.ts`'s threat-model section so port authors will use the explicit `followSymlinks: true` opt-in. | Samuel Combey |
| 2026-04-24 | T3.9 / Pit Crew M1 (Adversary 8) | Boundary-root case-sensitivity edge case on macOS APFS / NTFS / FAT32 deferred. | Adversary confirmed a case-mismatch between supplied `boundaryRoot` and on-disk canonical case can cause false-rejection on legit paths or false-allow on attacker paths if a "fix" relaxes the comparison. **Deferred to ADR-013** (alongside the IPC fs wrappers) where the boundary-canonicalization invariant has a natural home. Mitigation today: documented requirement that `boundaryRoot` be supplied in canonical case (typically the result of `realpathSync(process.cwd())`); the IPC module port at M5 will enforce this at the `runIpc()` envelope-parse step. | Samuel Combey |
| 2026-04-24 | T3.9 / Pit Crew M1 (QA 11) | Dead-code branch in `path-safety.ts` boundary-realpath catch is intentionally retained. | The branch (the `realpathSync(boundaryRoot)` catch at the symlink layer) is unreachable today because the candidate's realpath ENOENTs first. It becomes reachable when `assertInsideRoot` is called against a candidate that exists but a boundary that does not — an edge case that materializes once `runIpc()` is called with a misconfigured envelope. Removing the branch would silently allow that future configuration. Kept with a comment cross-referencing this entry. | Samuel Combey |
| 2026-04-24 | T3.9 / Pit Crew M1 (Rev M4) | `bin/lib-ts/path-safety.ts` + `bin/lib-ts/errors.ts` added to `tsdown.config.ts:entry`. | Originally only `_smoke.ts` was wired into the build. Without explicit entries, the path-safety Tier 2 dormant tests (per implementation-plan.md T3.9) cannot run against `dist/lib/path-safety.js` even after `bin/lib-ts/ipc.ts` lands. Adding the entries now means the dist artifact is verified by `check-dist-exports.mjs` from M1 onward, and Tier 2 activation happens automatically at M5 instead of requiring a follow-up tsdown.config edit. | Samuel Combey |

---

## Insights Reference

**Companion Document:** [`specs/insights/architecture-insights.md`](insights/architecture-insights.md) (Turn 1) + addendum entries for Turn 2 compilation

Key insights from this implementation plan:

1. **M5 correctly depends on both M3 and M4** — the Pit Crew Reviewer flagged that the original architecture table's `M5 → M4` arrow was incomplete; codebase-intelligence imports from spec/graph cluster, so M3 is also a prerequisite.
2. **ADR-002 depth-cost analysis is the FIRST task of M8 (T4.7.0)** — explicitly gated as `[Blocker]`. If commander→citty flip happens, the architecture.md + implementation-plan + Technology Stack entries get a coordinated sweep before T4.7.1 begins.
3. **Per-module port recipe is invariant across M2–M7** — **11 steps** (was 10; step 11 added per Pit Crew QA — IPC v0/v1 fixture authoring at port time, not deferred to M8) applied identically to every `bin/lib/<name>.js` port. This is deliberate: consistency lets Developer build muscle memory and lets CI gates run unchanged.
4. **KU-03 npm publish rights (T6.2) blocks M9** — if Samuel is not an owner on npm, 2.0 cannot ship under `jumpstart-mode` without author coordination OR adopting a scoped name. T6.2 runs in parallel from day 1.
5. **Security-hardening ADRs (009–012) are woven into specific port tasks** — ADR-010 zipslip lives in T4.5.1, ADR-011 LLM endpoint in T4.3.1, ADR-012 secrets redaction in T4.3.3. This avoids the "security is added at the end" anti-pattern.

---

## Final Summary (Completed by Developer Agent)

> _Developer Agent populates this section at end of Phase 4._

| Metric | Planned | Actual |
|--------|---------|--------|
| Total tasks completed | ~95 (verified row count post-Turn-2-Pit-Crew fixes; original "~110" estimate corrected per Adversary count check) | |
| Total milestones | 12 | |
| Schedule deviations | 0 baseline | |
| Risk triggers activated | 0 baseline | |
| ADRs added post-approval | 0 baseline | |

---

## Constraint Map Reference

All explicit constraints from Phase 0 Challenger Brief + Phase 1 Product Brief are preserved:

- ✅ Production-quality floor (hard constraint) — enforced by CI gates in M0 + M1
- ✅ CLI behavioral contract preserved — T2.7 snapshots + T4.7.3 diff gate + T4.7.4 IPC regression
- ✅ Config + state + installed.json shapes preserved — T4.1.12 + T4.5.3 historical-fixtures tests
- ✅ Stdin/stdout IPC envelope preserved — T4.7.4 backward-compat via v0/v1 fixtures
- ✅ Dual-mode library + subprocess pattern preserved — `ipc.ts` canonical pattern (T4.1.8)
- ✅ 83-test ratchet green throughout — every port checkpoint verifies
- ✅ Holodeck baseline green — T2.5 CI gate + every milestone checkpoint
- ✅ 9–12 month realistic timeline — 12 milestones; no compression assumed
- ✅ No feature additions — zero-behavior-change PR template (T1.4) + reviewer checklist

---

## Linked Data

```json-ld
{
  "@context": { "js": "https://jumpstart.dev/schema/" },
  "@type": "js:SpecArtifact",
  "@id": "js:implementation-plan",
  "js:phase": 3,
  "js:agent": "Architect",
  "js:status": "Approved",
  "js:version": "1.0.0",
  "js:upstream": [
    { "@id": "js:architecture" },
    { "@id": "js:prd" }
  ],
  "js:downstream": [
    { "@id": "js:developer-phase-output" }
  ]
}
```
